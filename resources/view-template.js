'use strict';

import * as core from '../views/view-core';
import * as util from '../views/view-util';

//-----------------------------------------------------------------------------
// HTML要素ID
//-----------------------------------------------------------------------------
{{#view-elements}}
const ID_{{id-descriptor}} = "{{id-value}}";
{{/view-elements}}

//-----------------------------------------------------------------------------
// HTML要素表示
//-----------------------------------------------------------------------------

// 全体を描画
export function renderAll(state) {
{{#render-elements}}
    render{{name}}(state);
{{/render-elements}}
    return null;
}

{{#render-elements}}
// {{comment}}を描画
export function render{{name}}(state) {
{{{view-code}}}
}

{{/render-elements}}

//-----------------------------------------------------------------------------
// HTML要素値取得
//-----------------------------------------------------------------------------

//-----------------------------------------------------------------------------
// 内部関数
//-----------------------------------------------------------------------------

//-----------------------------------------------------------------------------
// HTML要素とアクションのバインド
//-----------------------------------------------------------------------------

{{#view-elements}}
// {{comment}}をバインド
{{{bind-code}}}

{{/view-elements}}
